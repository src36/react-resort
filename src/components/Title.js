import React from 'react'

const Title = ({titleName}) => {
    return (
        <div className="section-title">
            <h4>{titleName}</h4>
            <div></div>
        </div>
    )
}

export default Title
